{{ form_start(form) }}
    <div class="row">
        <div class="col">
            {{ form_row(form.password.first) }}
        </div>
    </div>
    <div class="row">
        <div class="col">
            {{ form_row(form.password.second) }}
        </div>
    </div>
<a type="button" class="btn btn-secondary" href="{{ path('app_user_show', {'id': user.id}) }}">Retour</a>
    <button class="btn btn-primary">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
