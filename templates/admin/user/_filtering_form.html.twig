{{ form_start(form) }}
    <div class="d-flex justify-content-end">
        <div class="me-2">
            {{ form_row(form.firstname) }}
        </div>
        <div class="me-2">
            {{ form_row(form.lastname) }}
        </div>
        <div class="me-2">
            {{ form_row(form.email) }}
        </div>
        <div class="me-2">
            {{ form_row(form.role) }}
        </div>
        <div class="">
            <button class="btn btn-primary"><i class="bi bi-search"></i></button>
            <a class="btn btn-secondary" href="{{ path('app_admin_user_index') }}"><i class="bi bi-x-circle-fill"></i></a>
        </div>
    </div>
{{ form_end(form) }}
