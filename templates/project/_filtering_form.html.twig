{{ form_start(form) }}
    <div class="d-flex justify-content-end">
        <div class="me-2">
            {{ form_row(form.name) }}
        </div>
        <div class="">
            <button class="btn btn-primary"><i class="bi bi-search"></i></button>
            <a class="btn btn-secondary" href="{{ path('app_project_index') }}"><i class="bi bi-x-circle-fill"></i></a>
        </div>
    </div>
{{ form_end(form) }}
